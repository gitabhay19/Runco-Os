# Neon Postgres (or any Postgres provider)
# Get these from your Neon dashboard → Connection Details → Connection string
DATABASE_URL="postgresql://user:password@ep-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require"
DIRECT_URL="postgresql://user:password@ep-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require"

# NextAuth
NEXTAUTH_URL="https://os.runcogrowth.com"
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"

# Email (optional — logs to console if not set)
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
EMAIL_FROM="Runco OS <noreply@runcogrowth.com>"
